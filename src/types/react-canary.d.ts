// <ViewTransition> ships in the React build Next bundles for the App Router, so
// the route surface can use it at runtime; its types live in the canary surface
// of @types/react rather than the stable one.
/// <reference types="react/canary" />
